version: '3.8'
services:
  backend:
    build: ./server
    ports:
      - '8000:8000'
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
  frontend:
    build:
      context: ./elevatemy-path-main
    ports:
      - '3000:3000'
    environment:
      - VITE_API_URL=http://localhost:8000
